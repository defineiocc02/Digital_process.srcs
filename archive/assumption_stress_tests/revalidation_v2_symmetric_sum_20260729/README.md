# Revalidation v2 Archive

This directory preserves the superseded v2 formal output before the one-sided
headroom guard was added.

The v2 campaign showed that symmetric calibrated-weight sum normalization fixed
existing over-gain saturation tails but could scale other chips upward and
create new full-scale saturation. No file was deleted; the evidence remains
available for root-cause comparison. The qualified v3 result is under
`analysis/physical_cdac_mismatch_20260729/outputs_revalidation/`.
